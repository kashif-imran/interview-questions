package binarysearch;

public class ClosestNumberInSortedArray {/*
    public static void main(String[] args) {

        int[] input = new int[] {2,5,6,7,8,8,9};

        int target = 5;

        int size = input.length;
        int start = 0;
        int median = size/2;

        int result = findClosest(input, target, start, median, size);

    }

    public static int findClosest(int[] input, int target,
                                  int start, int median, int end) {

        int closest  = input[median];

        if(getDifference(target, input[median - 1]) < difference) {
            findClosest(input, target, start, median / 2, median);
        } else {
            median = ( median/2 + end)/2;
            findClosest(input, target, median /2, median, end);
        }

        return closest;
    }

    public static int getDifference(int target, int closest) {
        return Math.abs(target - closest);
    }
*/
}
